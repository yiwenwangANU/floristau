import Button from "@/components/ui/Button";

const CheckoutButton = () => {
  return (
    <div className="flex justify-center p-10">
      <Button variant="blackFull">Check Out</Button>
    </div>
  );
};
export default CheckoutButton;
