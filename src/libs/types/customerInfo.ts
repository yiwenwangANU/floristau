export type SenderState = {
  firstName: string;
  lastName: string;
  phone: string;
  email: string;
};

export type RecipientState = {
  firstName: string;
  lastName: string;
  phone: string;
  address: string;
  postcode: string;
};
